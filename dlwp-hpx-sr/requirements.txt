# Core (install PyTorch with the right CUDA build separately, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124 )
torch>=2.2
numpy>=1.26
matplotlib>=3.8
pyyaml>=6.0

# HEALPix mesh geometry (pixel centers, neighbour topology, interpolation)
astropy-healpix>=1.0

# ERA5 streaming from WeatherBench 2 public GCS (no credentials required)
xarray>=2024.0
zarr>=2.17
gcsfs>=2024.0
dask>=2024.0

# Optional logging
wandb>=0.17          # opt-in; enable via config wandb.enabled: true
