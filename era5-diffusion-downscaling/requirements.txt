# Core (install PyTorch with the right CUDA build separately, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124 )
torch>=2.2
numpy>=1.26
scipy>=1.11
matplotlib>=3.8
pyyaml>=6.0
tqdm>=4.66

# ERA5 streaming from WeatherBench 2 public GCS (no credentials required)
xarray>=2024.0
zarr>=2.17
gcsfs>=2024.0
dask>=2024.0

# Optional logging
tensorboard>=2.16
